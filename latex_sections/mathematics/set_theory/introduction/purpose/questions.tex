\begin{enumerate}
  \item In your own words, what is a \textbf{set}? What does it mean to call a set an \emph{abstract container}?

  \item Why are sets useful for mathematicians and engineers when analyzing or categorizing objects?

  \item Explain the programming analogy used in the notes: how is a set similar to wrapping many function parameters into a \texttt{struct} or \texttt{class}?

  \item In the expression $A = \{4, 6, 1, -3, 14, 99\}$, what do the curly braces mean and what does the label $A$ represent?

  \item Give an example of a set whose elements are not numbers (like the color set). What kinds of objects can be elements of a set?

  \item What does it mean for a set to contain other sets? Use the example of $C$ to describe a \emph{set of sets}.

  \item Create your own example of a set that contains at least one set as an element.

  \item How does naming a set (giving it an identifier like $A$ or $B$) help you communicate and reason about a collection of objects?
\end{enumerate}

